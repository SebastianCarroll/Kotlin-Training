package com.functions

/*
Use .asSequence to convert a list to a sequence

Sequences have transformation and terminal operations
Nothing will happen if no terminal operations are called

Lazy evaluated

Can pass around a partially evaluated stream without operating on it until we operate with the
terminal op
 */